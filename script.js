// Exemplo de um código simples para interatividade
document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Sua mensagem foi enviada!');
});
